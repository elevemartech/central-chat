
  # High-Fidelity Chat Interface

  This is a code bundle for High-Fidelity Chat Interface. The original project is available at https://www.figma.com/design/hBKavBX3blZvRBHi0QEqtA/High-Fidelity-Chat-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  